<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tmlstore_listview_item"
    android:layout_width="fill_parent"
    android:layout_height="100dp"
    android:background="@android:color/white" >

    <ImageView
        android:id="@+id/tmlstore_listview_item_iv"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_alignParentLeft="true"
        android:padding="5dp"
        android:src="@drawable/tml_store_listview_item_image" />

    <TextView
        android:id="@+id/tmlstore_listview_item_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/tmlstore_listview_item_iv"
        android:padding="5dp"
        android:text="冰酷330ml"
        android:textColor="@color/normal_textcolor1" />

    <ImageView
        android:id="@+id/tmlstore_listview_item_arrow"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="25dp"
        android:src="@drawable/yitong_login_right" />

    <RelativeLayout
        android:layout_width="50dp"
        android:layout_height="match_parent"
        android:layout_marginRight="10dp"
        android:layout_toLeftOf="@+id/tmlstore_listview_item_arrow" >

        <com.yitong.widget.CircleChart02View
            android:id="@+id/circle_view"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_centerVertical="true" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/circle_view"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="18dp"
            android:text="库存率"
            android:textColor="@color/normal_textcolor1" />
    </RelativeLayout>

    <!-- 这个 TextView 在显示的时候设置不可见，其作用是用来记录 sku 的服务器 objectId,这样的点击后跳转的时候将 objectId 传递过去 -->

    <TextView
        android:id="@+id/tmlstore_show_sku_item_objectid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone" />

</RelativeLayout>